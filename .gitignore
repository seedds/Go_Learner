# Xcode / build
# GoLearner.xcodeproj is generated by `xcodegen generate` (see note below).
GoLearner.xcodeproj/
DerivedData/
build/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# Notes:
# - GoLearner.xcodeproj is generated from project.yml; do not commit it.
# - Resources/KataGoModel19x19fp16.mlpackage (~55 MB) is a bundled build input.
#   Keep it if you want a one-clone build; move to Git LFS or a download step
#   if repo size matters.
